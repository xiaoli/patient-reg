==========
Deployment
==========
virtualenv venv
source venv/bin/activate

pip install -r requirements.txt

python venv/bin/django-admin.py startproject patient_reg [Only needed for creating new project]
